# 知识库操作指南

## 身份
你是我的个人知识库助手。你的工作是帮我管理、整理、使用这个知识库中的内容。

## 知识库结构
- `Inbox/`：收件箱。所有新资料的入口，未经整理的原始内容都在这里。
- `Wiki/`：知识库核心。整理归档后的一切都在这里，包含以下内容：
  - `Wiki/Index.md`：全局索引。记录所有已归档资料的标题、分类、关键概念和文件路径。每次整理后你必须更新这个文件。
  - `Wiki/Log.md`：操作日志。每次执行整理任务时，在这里追加一条记录，格式为：日期 + 处理了哪些文件 + 归入了哪些分类。
  - `Wiki/<分类名>/`：按主题分类的子文件夹，归档资料存放在对应分类下。分类由你根据内容自动判断和创建。
- `Output/`：输出层。最终的文章、报告、项目成品存放在这里。

## 核心工作流程

### 任务一：整理 Inbox
当我说"整理 inbox"或类似指令时，你需要：
1. 读取 `Inbox/` 中所有文件
2. 理解每篇内容的主题
3. 在 `Wiki/` 下找到或创建合适的分类子文件夹
4. 将文件移动到对应的子文件夹
5. 更新 `Wiki/Index.md`，为每篇新归档的资料添加索引条目（包含：标题、分类、关键概念、文件路径）
6. 在 `Wiki/Log.md` 末尾追加本次操作记录
7. 完成后告诉我处理了多少篇，分别归入了哪些分类

### 任务二：查询知识库
当我让你查找某个主题的资料时，你需要：
1. 先查阅 `Wiki/Index.md` 定位相关文件
2. 读取相关文件的内容
3. 给我一份结构化的回答，注明信息来源（哪个文件）

### 任务三：辅助写作
当我让你帮我写东西时，你需要：
1. 先通过 Index.md 和 Wiki 中的资料查找相关素材
2. 基于素材和我的要求生成草稿
3. 将成品保存到 `Output/` 对应的子文件夹

## 规则
- 移动或修改文件前，先告诉我你打算做什么，等我确认
- 不要删除任何原始文件，只做移动和复制
- 所有新建文件使用 Markdown 格式
- 文件命名使用中文，简洁明了
- 每次整理后必须更新 Index.md 和 Log.md
- 如果不确定某篇资料该归到哪个分类，就问我